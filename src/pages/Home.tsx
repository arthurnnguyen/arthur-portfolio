import Layout from "../components/Layout";
import Hero from "../components/Hero";

export default function Home() {
    return (
        <Layout>
          <Hero />
          {/* add more sections later, e.g. <Skills/> <Testimonials/> */}
        </Layout>
      );
  }